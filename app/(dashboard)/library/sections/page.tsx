import ComingSoon from "@/components/library/ComingSoon";

export default function LibrarySectionsPage() {
  return (
    <ComingSoon
      title="Sections library"
      description="Reusable workout sections — warm-ups, supersets, finishers — coming soon. For now, sections are managed inside individual workouts."
    />
  );
}
